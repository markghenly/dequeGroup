// src/main.jsx
import React from 'react';
import ReactDOM from 'react-dom';
import './App.css';  // Update this line to the correct relative path
import App from './App';

ReactDOM.createRoot(document.getElementById('root')).render(
  <React.StrictMode>
    <App />
  </React.StrictMode>,
);
